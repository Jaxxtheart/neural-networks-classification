import ComingSoonPage from "@/components/shared/ComingSoonPage";

export default function ConnectorsPage() {
  return (
    <ComingSoonPage
      title="Connectors"
      description="Configure data sources — databases, cloud storage, streams, and APIs."
    />
  );
}
